BUFR format definition for CODA
-------------------------------

This codadef provides the CODA format definition for the raw BUFR format.
It is not meant to read the actual data that is encoded into BUFR messages
but rather to inspect raw BUFR messages itself.
